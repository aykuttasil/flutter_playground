import 'package:flutter/material.dart';

class PageRouteBuilderPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('PageRouteBuilderPage'),
      ),
      body: Container(
        child: Text('PageRouteBuilderPage'),
      ),
    );
  }
}
